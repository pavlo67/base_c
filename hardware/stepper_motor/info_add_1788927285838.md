# API correction

The documented free sequence runner was outdated. `executeSequence()` has been removed; callers and tests use `StepperMotorAction::run()` and `result()` directly. `stepperMotorRun()` preserves cleanup before GPIO termination. Diagnostics use `[function()] ERROR: details`, with qualified function names where needed.
