# Statistics interval addition

The shared runner now includes `expecterInterval` in milliseconds in every clocked/real phase block total and TOTAL row. It denotes requested scheduler cadence; real uses 0.001 ms when the configured interval is zero. See the Scheduler interval in statistics section of info.md.
