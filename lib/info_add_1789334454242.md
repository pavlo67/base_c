# Timing documentation correction

The timing files are `timelib.h/.cpp`, not `time.h/.cpp`.
The shared `Clock` now supplies monotonic timestamps to the runner and
`monotonicNowMs()` / `internal32Ms()` without a hardware dependency.
